<% if @created %>
  $("#users-list").append("<%= j render partial: '/users/item', locals: { user: @user } %>");
  $('#user-modal').modal('hide');
  toastr.success('<%= tm(user, :user_created) %>')
<% else %>
  toastr.danger('<%= tgm(:error) %>')
<% end %>


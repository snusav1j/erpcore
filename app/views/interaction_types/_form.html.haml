= simple_form_for @interaction_type, remote: true, html: { id: 'interaction-type-form', novalidate: false } do |f|
  .form
    .row
      .col-md-12
        = f.input :name, required: true
    .row
      .col-md-12
        = f.input :color, input_html: { class: 'colorpicker-input' }
        %div.colorpicker
<% if @created %>
  $("#orders-list").append("<%= j render partial: '/orders/item', locals: { order: @order, columns: @columns } %>");
  $('#order-modal').modal('hide');
  toastr.success('<%= tm(Order, :order_created) %>')
<% else %>
  toastr.danger('<%= tgm(:error) %>')
<% end %>

